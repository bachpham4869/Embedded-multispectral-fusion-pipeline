# models/ablation — training artifacts (9 vs 12 feature)

- **`rf_optical_9.joblib`**, **`rf_optical_12.joblib`** (+ sidecar `.json`) — ablation bundles for thesis evaluation.
- **Reliability diagram PNGs** produced during training are stored under **`docs/figures/ml/ablation/`** (repo layout); this folder keeps only model weights and metrics sidecars.
