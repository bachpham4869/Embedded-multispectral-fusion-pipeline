# src/ — Package Python

- **`smartbinocular/`** — mã nguồn cài đặt qua `pip install -e .` (xem `pyproject.toml`).
- Entry: `python -m smartbinocular` hoặc console script `smartbinocular`.

Tổng quan module: [`README.md`](../README.md) (root) — mục *Module Structure* và *Data Flow*. Sprint & metrics ghi chú: [`../weekly_report/week_3.md`](../weekly_report/week_3.md).

Lịch sử plan BGR / indoor / saturation (archive): [`legacy/md/PLAN_INDOOR_BGR_SATURATION_TRACE.md`](../legacy/md/PLAN_INDOOR_BGR_SATURATION_TRACE.md).
