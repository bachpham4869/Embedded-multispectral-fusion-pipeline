"""
SmartBinocular — real-time NIR and thermal fusion for handheld night vision.

The importable package exposes processing and hardware helpers under
``smartbinocular.*``. The live application loop is started only through
``python -m smartbinocular`` (see ``__main__.py`` → ``main.main()``).
"""
