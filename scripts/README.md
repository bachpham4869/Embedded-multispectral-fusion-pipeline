# scripts/ — Tiện ích shell (cục bộ)

Script hỗ trợ tải dataset (ví dụ `fetch_weather_datasets.sh`). Thư mục có thể bị **loại khỏi git** theo `.gitignore` root; nếu clone không có script, lấy từ nhánh nội bộ hoặc tái tạo theo `docs/DATASET_LOCAL_STATUS.md`.
